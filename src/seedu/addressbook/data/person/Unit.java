package seedu.addressbook.data.person;


/**
 * Represents a Person's unit in the address book.
 */

public class Unit extends AddressComponent {

    public Unit(String component) {
        super(component);
    }

}

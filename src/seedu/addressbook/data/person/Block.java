package seedu.addressbook.data.person;


/**
 * Represents a Person's block in the address book.
 */

public class Block extends AddressComponent {

    public Block(String component) {
        super(component);
    }

}
